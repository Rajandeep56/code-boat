
package com.xfeatures.exercise;
import java.util.*;
import java.lang.*;


public class ex2_ifelse_day1 {
    public static void main (String[] args){
        /*
        (Algebra: solve 2 * 2 linear equations) A linear equation can be solved using
Cramer’s rule given in Programming Exercise 1.13. Write a program that prompts
the user to enter a, b, c, d, e, and f and displays the result. If ad - bc is 0, report
that “The equation has no solution.”
        */
    Scanner sc = new Scanner (System.in);
    System.out.println("please enter the values of a b c d");
    float a = sc.nextFloat();
    float b = sc.nextFloat();
    float c = sc.nextFloat();
    float d = sc.nextFloat();
    
    //validation
    
    float z = (a*d)- ( b *c);
    if (z==0){
    System.out.println("the equation has no solution");
    }
    else 
        System.out.println("you answer is: " +z );
    
    }
    
    
}
