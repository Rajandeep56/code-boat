
import java.util.*;
import java.lang.*;
public class loopsex_5_1 {
    
    public static void main (String[] args){
    /*
        5.1 (Count positive and negative numbers and compute the average of numbers) Write
a program that reads an unspecified number of integers, determines how many
positive and negative values have been read, and computes the total and average of
the input values (not counting zeros). Your program ends with the input 0. Display
the average as a floating-point number. Here is a sample run:
        */
    Scanner sc = new Scanner(System.in);
    System.out.println("please enter any number");
    int input = sc.nextInt();
    int count=0;
    int negcount =0;
    int total=0;
    int totalCount=0;
        
    
    while(input !=0){
        totalCount++;
         total += input;
         
    if (input>0){
    count++;
    //System.out.println(count);
    
    }
    if (input<0){
    negcount++;
    //System.out.println(negcount++);
    }
    input = sc.nextInt();
   
    }
    float avg = (float)total / totalCount;
    System.out.println("you input total of numbers are: "+ totalCount + " and count for positive numbers is "+count +" negative is"+ negcount);
   System.out.println("your average is: "+ avg);
    System.out.println("program ended");
    
    
    
}}
